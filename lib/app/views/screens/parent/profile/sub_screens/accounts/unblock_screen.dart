import 'package:flutter/material.dart';

class UnblockScreen extends StatelessWidget {
  const UnblockScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}